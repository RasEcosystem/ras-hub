# Agent instructions

Project-specific agent guidance is stored locally in `docs/agents/`:

- read `docs/agents/project.md` before working in this repository;
- also read `docs/agents/background-tasks.md` before changing `src/RasHub.BackgroundTasks` or its integration tests.

The `docs/agents/` directory is intentionally excluded from Git.
